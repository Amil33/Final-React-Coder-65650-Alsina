import React from 'react';

const CartWidget = () => {
    return (
        <div>
            🛒 <span>0</span> {/* Representa la cantidad de items en el carrito */}
        </div>
    );
};

export default CartWidget;

